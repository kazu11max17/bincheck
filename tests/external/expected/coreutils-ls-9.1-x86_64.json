{
  "_comment": "Expected output for verify-external.sh. Match observed bincheck 0.3.1 output.",
  "binary": "coreutils-ls",
  "version": "9.1-1",
  "arch": "x86_64",
  "expected": {
    "linkage": "dynamic",
    "libc": "glibc",
    "fortify_source": true
  },
  "_notes": "Debian-built dynamic glibc `ls` with _FORTIFY_SOURCE=2. Bare strcpy import remains in dynsym alongside __strcpy_chk; F1 detects the bare variant per spec L84."
}
