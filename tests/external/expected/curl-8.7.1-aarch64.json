{
  "_comment": "Expected output for verify-external.sh. Match observed bincheck 0.3.1 output.",
  "binary": "curl",
  "version": "8.7.1",
  "arch": "aarch64",
  "expected": {
    "linkage": "static",
    "libc": "none",
    "fortify_source": false
  },
  "_notes": "static-curl is a musl-static aarch64 build. Because the binary has no DT_NEEDED entries, F5 reports `none`. Stripped static binary, no banned-function symbols visible to F1."
}
