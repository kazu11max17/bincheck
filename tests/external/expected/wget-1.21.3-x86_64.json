{
  "_comment": "Expected output for verify-external.sh. Match observed bincheck 0.3.1 output.",
  "binary": "wget",
  "version": "1.21.3-1+deb12u1",
  "arch": "x86_64",
  "expected": {
    "linkage": "dynamic",
    "libc": "glibc",
    "fortify_source": true
  },
  "_notes": "Debian-built dynamic glibc wget with _FORTIFY_SOURCE=2. Despite fortify, bare strcpy and sprintf imports remain in dynsym (per spec L84: bare imports are reported even when _chk variants coexist)."
}
